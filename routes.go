package ai

import (
	"github.com/gofiber/fiber/v2"
)

// setupRoutes registers the plugin's API routes
// TODO: Implement routes with updated database API (requires context.Context)
func setupRoutes(app *fiber.App, plugin *Plugin) {
	// Routes will be implemented after migration to new database API
}
